# Scope decisions

**What this file is.** The register of what is deliberately *not* built, and why.
`tendril-spec.md` §10 stopped tracking this on 2026-09-06 — correctly, since an inline
register of that size goes stale faster than it can be read — but it then declined to
name where the worklist had gone. It had gone into a session scratchpad outside version
control. This file is the answer §10 refused to give.

**Reading rule.** Every row says how its status was established. `verified` means measured
against the tree on the date given. `asserted` means a plan document claims it and nobody
has checked. Do not promote an `asserted` row to fact by citing it.

---

## Rulings — 2026-09-09

| # | Item | Ruling | Note |
|---|---|---|---|
| **P2** | IMAGE blocks are offered, imported and stored — and never drawn | **Build — done in code; the on-device acceptance run not yet done** | Three separate holes, all closed. **Rendering:** `decodeImageBitmap` as `expect`/`actual` (Android `BitmapFactory`, desktop Skia) — only the decode is platform-specific, so reading, caching, threading and failure handling are written once. Decoding runs off the composition thread via `produceState`: a block image is arbitrarily large and decoding one inside composition would drop frames on the page being scrolled. `BlockRow` draws it **above** the text field, matching how CODE renders its language label — the field stays because the Notion importer stores an image block's alt text as its content, and drawing the image *instead* would make that uneditable and invisible at once. **`GalleryBody`:** its `if (cover == null)` had no `else`, so a row *with* a cover drew nothing — the one case a gallery exists to show was the blank one. Covers take `ContentScale.Crop` (a thumbnail standing in for a page, whole image one tap away, uniform grid) while a block takes `Fit` (the picture *is* the content, and trimming its edges loses what it was photographed for). **The picker:** `expect`/`actual` returning a `() -> Unit` launcher rather than taking a show-flag, because the platforms disagree about what showing means — Android's is an Activity result registered during composition and fired later, desktop's a modal AWT dialog that blocks where it is called. It hands back **bytes, not a Uri or path**: a content Uri is not readable later and a path may be unreachable next launch, so the copy into app-private storage happens while the grant is live, the same rule `NotionImporter` follows. `PickVisualMedia` needs no storage permission at all. The stored file is named after the **block uid**, exactly what S4's sync calls it, so an inserted image and a fetched one are indistinguishable on disk — which is what makes the round trip work. `LocalImageStore` moved from `jvmCommon` to `commonMain`: it had been placed beside `SyncFileStore` by resemblance, but `WorkbenchCore` and `PageDetailViewModel` are common code and cannot see `jvmCommon`; nothing in the interface is platform-specific. One dependency added — `androidx.activity:activity-compose` on `shared`'s androidMain, already in the app's catalog. S4 also made a UI distinction worth drawing: a block with no local file now means either "no picture" or "not arrived from the folder yet", and the placeholder says which. 497 JVM tests, 0 failures; Android, Windows and `shared` build; `tools/audit.py` clean. **Acceptance test run on hardware 2026-09-09** (OnePlus 9 Pro, debug build), and it found three defects the 497 passing tests could not — this project has no Compose UI test harness (recorded under B3), so nothing in the suite exercises a line of this UI. **(1) "Image" was in the slash menu and unreachable.** `SlashCommandSheet`'s `Column` had no scroll, so the sheet clipped whatever did not fit, and what did not fit was the last entry. That is the literal explanation for P2's own title: the type was "offered" only in source. Fixed with `verticalScroll`. **(2) The stored file had no extension.** A photo-picker Uri's last segment is a bare media id with no dot, so deriving one from the "file name" yielded nothing. Harmless for decoding (which reads bytes) and for S4's uid recovery (`substringBeforeLast('.')` returns the whole string with no dot), but it defeats the reason the extension travels at all. Now taken from `contentResolver.getType(uri)` via `MimeTypeMap`. **(3) Replacing an image with a different file type orphans the old copy** — the name changes, so it does not overwrite in place. Introduced by this change and caught by the same run; the code comment that claimed otherwise is corrected, and the cleanup belongs with S4's purge step. **What the run confirmed:** insert through the system picker with a scoped grant and no storage permission; the file stored app-private as `<block-uid>.png`; the block drawing it, with "Replace image" replacing "Choose image"; survival across reinstall; publication to a real SAF folder as `images/<block-uid>.png`, **byte-identical to the local copy** (207,275 bytes, matching SHA-256); the page record carrying `imageName` matching that file; and — after deleting the local copy and nulling `imagePath` — the next sync **fetching it back and re-attaching a path this device derived itself**, then rendering it. That last step is S4's receive half and its first exercise against real SAF rather than `InMemorySyncFileStore`. **Still not verified:** desktop rendering, which is compile-only. | 
| **S4** | Images are in no snapshot and no portable export | **Build — the folder channel done (stages 1–3); archive and purge still open** | **Two predictions in the 2026-09-09 P2 scoping row below turned out wrong, both in the cheaper direction.** That row says the snapshot "needs a content-addressed name plus per-device path resolution, which is a different shape from just relaxing the current exclusion, and it inverts the very invariant `PageMergeTest`'s existing test locks in". Neither held. **No schema change was needed**: name the folder file after the *block uid* — `images/<block-uid>.<ext>` — and every piece already exists, since the block has a uid, `imagePath` already says this device holds the file, and `BlockSnapshotRecord` is JSON rather than schema so a defaulted field costs nothing. That is also what Joplin core does (resource *ids*; its deduplication is a plugin) — content addressing buys dedup and costs a column, uid addressing buys simpler replacement and deletion and costs nothing. **And the invariant does not invert**: it holds exactly, because the two fields answer different questions. `imageName` says *which* image and travels; `imagePath` says where this device keeps it and still never leaves, so the uid re-attach is still the only thing carrying a local path across a rebuild. Only that test's justification needed a nuance. **Built:** a third `images/` channel on `SyncFileStore` (already byte-oriented, so binary needed no new abstraction) across all three stores; `BlockSnapshotRecord.imageName`; a separate `LocalImageStore` for app-private storage, deliberately *not* a fourth channel on `SyncFileStore` — one interface would have made it easy to write the merge that copies a peer's absolute path into local state, which is the bug the separation makes unspellable; publish-on-write and fetch-on-merge in the orchestrator. **Fetching is driven by the folder's `images/` listing, not by the merged records** — the name embeds the block uid, so the listing already says everything needed, an image and its page record may arrive in either order, and a failed fetch is simply retried next pass. Publishing skips a name the folder already holds, since Syncthing replicates every change and identical bytes each pass are pure churn. **Recorded gap, not fixed here: image bytes are written unencrypted even when the folder key is set.** §9.4.2's scheme wraps text payloads and an image is the one thing here that is not one, so an encrypted folder currently protects the note that mentions a picture and not the picture. That wants its own decision rather than a quiet fix buried in S4. 6 tests, the load-bearing one being that a peer's path is never adopted — a merge that copied one would look correct on a single device and break on every real pair. 497 JVM tests, 0 failures; Android, Windows and `shared` build; `tools/audit.py` clean. **Still open:** `PortableArchive` (its `readZipEntries` decodes every entry to `String`, so images need a parallel `ByteArray` map through decode, quarantine and the 128 MB cap — which images will actually reach rather than merely guard against), and purging an image once its block is gone. Both are additive consumers of this channel that change neither the interface nor the on-disk format, which is why they are separate. P2 (rendering) is unblocked by this. | 
| **S1b** | Remove `fallbackToDestructiveMigration(dropAllTables = true)` | **Narrowed rather than removed — done, hardware-verified** | S1's row says "delete"; the change that shipped narrows instead, to `fallbackToDestructiveMigrationFrom(dropAllTables = true, 1..7)`. **Deleting outright is the wrong shape.** §9.10's policy is explicitly two-part — destructive pre-v1, snapshot-backed recovery post-v1 — and v9 is where the second half began: first declared migration, first version holding data that is not "trivially re-creatable" in §9.10's own words. A database still at v1–v7 belongs to a build predating all of that and will never have a migration written for it; handing it to `openOrRecover` would faithfully preserve a file whose contents nothing wants and nothing can read, kept forever. **What changes for v9 onward is the point of the item**: a bump whose migration was forgotten, a downgrade from a newer build, and a schema whose hash moved while its version did not all used to end in a silent destructive wipe (the last one in a crash §9.10 notes the fallback "never gets the chance to recover" from). All three now reach `openOrRecover`, which renames the database aside rather than destroying it and reports through `AppContainer.databaseWasRecovered` — strictly more recoverable in every case. That closes the failure §9.10's acceptance names by name: "never a silent `fallbackToDestructiveMigration()` left in place after the first release". **An earlier draft of this change crashed the app on every launch and the JVM suite could not see it.** `PRE_RELEASE_VERSIONS` initially included 8; Room rejects that at `build()` ("Inconsistency detected. A Migration was supplied to addMigration() that has a start or end version equal to a start version supplied to fallbackToDestructiveMigrationFrom()") because `MIGRATION_8_9` declares a path *from* 8 — and it was wrong on the merits too, v8 being the one pre-v9 version whose data must never be dropped. 485 tests passed throughout: nothing in the suite constructs a real Room database, so this was compile-green and test-green while being dead on device. Found by the hardware check, not by review. **Hardware verification (OnePlus 9 Pro, 2026-09-09):** a populated v10 database met the v9 build, and where the blanket fallback had wiped it the day before, it was preserved — `tendril.db.unopenable-1788959136514` opened afterwards still reading v10 with both rows, beside a fresh empty v9 the app started on, no crash. **Not covered:** the v1–v7 destructive path itself, which would need a pre-release database with a matching identity hash manufactured to exercise — more work than the change, and by construction those versions predate any install that still exists. | 
| **S3** | Test the restore fallback — "it has never been wired or exercised" | **Build — done, hardware run included** | The build-order row says "never been wired or exercised" and the emphasis belongs on the first word: **there was no fallback to test.** Nothing caught a failure opening the database — `AppContainer` and the desktop `main` both assigned `buildTendrilDatabase(...)` straight into a field, and no `catch`, and no "reset local data" dialog, exists anywhere in the tree. Room opens lazily, so a migration that throws surfaces at the first DAO call and takes the app down on every launch with no route back. **`fallbackToDestructiveMigration` does not cover it, and §9.10 says so itself — "it only handles version *changes*"** — it fires when no path is declared, whereas a declared migration that throws is the opposite case. **S2 made this reachable for the first time**: v9 is the first version in this app's history with a declared migration. Three other candidate holes were checked and are not holes: both platforms already run read-merge-then-write and guard the passphrase-mismatch case (no bare-write path exists that could publish an emptied device's state over the folder), and `PagesSyncEngine` rebuilds the FTS index on merge, so a recovered database is searchable. `openOrRecover` (jvmCommon) builds, **probes with a real query** — lazy open means building proves nothing — and on failure closes, sets aside, rebuilds and re-probes; a second failure rethrows rather than looping, since a freshly created database that will not open means the old file was not the problem and setting another aside would destroy a working copy. **The unopenable file is renamed aside, not deleted** (`-wal` and `-shm` with it — a stale WAL beside a fresh `tendril.db` is a worse failure than the one being recovered from). That is a deliberate divergence from §9.10's "gate it behind a confirm dialog": a dialog needs a running app and there is no database to run on, and setting the file aside answers the same concern by the rule `.tendril-lost-` already encodes. `AppContainer.databaseWasRecovered` carries the fact out, because after recovery the app is indistinguishable from a fresh install and someone not told will read a working sync as data loss. 8 tests: four on the ordering (close before set-aside; a close that itself throws must not block recovery; a second failure rethrows) and four on the outcome, the load-bearing one being that **a recovering device must not publish its emptiness** — every device rewrites each folder file wholesale, so the device with nothing left to lose is the one that could strip the folder for everyone — plus the honest limit that what was never published does not come back. **Run on hardware 2026-09-09 — OnePlus 9 Pro (LE2123), Android 14, debug build (`com.tendril.app.debug`, its own data directory).** Sequence and result: schema v9 created on device with `reminders` carrying both `uid` and `deletedAt`, so `MIGRATION_8_9` produced the intended shape against real SQLite; the database populated to 3 entries, 2 reminders (one tombstoned) and 1 completion, and the app read it (Tasks showed "Undated (3)"); a sync wrote `reminders.json` and `entry_completions.json` into the folder with every record naming its Entry by uid, `anchorTime` as "09:00", `occurrenceDate` as "2026-09-10" and the tombstone's `deletedAt` present. Then a build whose v9→v10 migration throws was installed over it. **The app stayed up.** `tendril.db` became `tendril.db.unopenable-1788951847945` with its `-wal` and `-shm` moved alongside, a fresh v10 database was created, and the set-aside file — pulled and opened — still held all three entries, both reminders with the tombstone intact, and the completion. The launch sync then repopulated everything from the folder, tombstone included, so the deleted reminder did **not** resurrect. **The folder was unharmed by the emptied device's own write pass** — byte sizes and contents identical afterwards — which is `emptyDeviceDoesNotPublishItsEmptiness` confirmed on hardware rather than in a fake. Incidentally also exercised: the reverted v9 build opening the v10 database, a downgrade, which `fallbackToDestructiveMigration` absorbed and the next sync refilled. **Limits of this run, stated so a green result is not read as more than it is:** `openOrRecover` catches a migration that *throws*; it does not catch lower-level file corruption that makes SQLite refuse the file before Room's migration machinery runs, and that path is still untested. One device, one debug build. `tendril-spec.md` corrected in two places: §4's Reminder row (still said reminders are device-local and destroyed by Restore — S2 fixed that) and §9.10's acceptance block. | 
| **S2** | Reminder + EntryCompletion reach the other device at all | **Build — done, both halves** | Picked up 2026-09-09 as the root of the remaining SYNC lane, after establishing that **`build-order.md`'s dependency order is misleading here**. It lists S2 as depending on S1, and `scope-decisions.md` records S1 as split with **S1b not done** — which reads as a block but is not one. What S2 actually needs from S1 is S1a: Room synthesises an `@AutoMigration` by diffing the previous version's JSON, so the committed `schemas/…8.json` is the hard prerequisite, and that shipped 2026-09-08. `fallbackToDestructiveMigration` is the fallback for a version with *no* declared path, so adding a real 8→9 path means it never fires for that upgrade. **S1b is downstream of S2, not upstream**: its stated blocker is that the snapshot-restore recovery meant to replace the destructive fallback has never been exercised against a deliberately-broken migration on a populated device — and until v9 there was no migration to break. That is S3's acceptance test. Corrected chain: S1a → **S2** → S3 → S1b → S4 → P2. | 
| **S2**, stage 1 | The v9 schema and the first real migration | **Done — `af39f7b`** | `uid` on both entities (the second-position, `Index("uid", unique = true)` shape all eleven other uid-carrying entities use), `version = 9`, `MIGRATION_8_9`. **`@AutoMigration`, which the S2 row specifies, cannot express this migration** — measured, not assumed: Room requires a SQL `defaultValue` for a new NOT NULL column, SQLite forbids a non-deterministic expression in a `DEFAULT` clause (exactly what a per-row UUID needs), so every pre-existing row would take the same literal and the unique index could not then be built on any device with two reminders; `AutoMigrationSpec.onPostMigrate` does not rescue it, because the indices are created before it runs. Hand-written, ordering add → backfill → unique index. The backfill builds a UUID-v4-shaped string inside SQLite (36 chars, hex and hyphens, matching what `Property.kt` documents a uid to be) using `random() & 3` rather than `abs(random()) % 4`, since SQLite's `abs()` overflows on exactly one of its 2^64 inputs. The column is added `DEFAULT ''` only so the `ALTER` is legal; the entity declares no default and Room's open-time validation only compares a default it learned from the entity, verified against the generated `9.json` (`defaultValue: null` on both). Destructive fallback deliberately kept — removing it is S1b, still open on its own terms. **Also fixed here, and not a cosmetic build issue:** `kspAndroidMain` and `kspKotlinDesktop` both write `schemas/<version>.json` and `org.gradle.parallel=true` let them do it at once, so one read the file while the other had truncated it (`Expected start of the object '{', but had 'EOF' instead`). Latent until a version actually moves — a file already present and current is never rewritten — so v9, the first bump since `exportSchema = true` landed, is the first build that could ever hit it, and every future bump would have hit it identically. Ordered rather than given a copy each: two schema files that could silently disagree is the failure §9.10 is trying to avoid. Confirmed by controlled test — `9.json` deleted and the build re-run under the conditions that failed, with the ordering as the only change. Verification: `shared`, Android (`assembleDebug` + `assembleDebugAndroidTest`) and Windows build; 469 JVM tests unchanged; `tools/audit.py` clean; `9.json` diffed structurally against `8.json` — three columns and two unique indices added, nothing removed or altered. | 
| **S2**, stage 2 | The snapshot records, mappers and merge | **Done** | `ReminderSnapshotRecord` and `EntryCompletionSnapshotRecord`, both mappers, two new `FolderArrayFile` members (`reminders.json`, `entry_completions.json`) and their merges. **Neither is a last-write-wins merge, and neither needs to be** — which the one-line S2 row does not anticipate. `EntryCompletionDao` has only an insert and a read: append-only, so the merge is a plain union by uid, a grow-only set that converges in any arrival order. `ReminderDao` had an insert and a hard `DELETE`, no update at all — so a uid is created once and deleted once, and the merge is monotonic without any timestamp to compare. That same absence was the danger: every device rewrites its array file in full, so a reminder deleted on A and still live in B's copy would be re-adopted, and `AlarmScheduler` reads those rows — a resurrected reminder **re-registers an alarm and fires**. Ruled 2026-09-09 (user decision, three options weighed): `Reminder` gains a `deletedAt` tombstone, folded into v9 while the migration was still unmerged so it cost no extra schema version. The `deletedAt IS NULL` filter lives in the DAO's two per-entry reads, so no existing caller had to change and a tombstone cannot reach `AlarmScheduler` through any query it already used; `getAll`/`getByUid` are deliberately unfiltered, because the tombstone has to keep travelling. **Tombstones need no collector**: `PurgeRegistry` hard-deletes a purged Entry and `ForeignKey.CASCADE` takes its reminders. That matters because the standard CRDT condition for safe tombstone GC — every replica has seen the marker — is unavailable to a Syncthing folder, where devices are anonymous and the replica count is unknown. A record whose Entry has not merged locally yet is **held and republished verbatim**, not dropped: dropping is safe for `originalEntryUid` because it self-heals, and unsafe here, because the very next write would delete the peer's record from the folder along with the copy that would have healed it. An unreadable offset quarantines that one reminder — deliberately stricter than `Converters.stringToReminderOffset`, which falls back to `ONE_DAY`; that fallback is right for a non-null column this device wrote itself and wrong for a peer's record, where it would silently change when an alarm fires. 7 new tests in `ReminderCompletionSyncTest`; 476 JVM tests, 0 failures; Android and Windows build; `tools/audit.py` clean. | 
| **S2**, stage 3 | `PortableArchive` — the `.tendril` half of the acceptance test | **Done** | Export writes `reminders.json` and `entry_completions.json`, encrypted with the rest and listed in the manifest; `importAdditive` and `restoreFromBackup` apply both **after** the entries, since both resolve `entryUid` against rows `applyEntries` may only just have inserted. Merge rules are the folder sync's, with one deliberate divergence: a record whose Entry is absent is *held* by the folder sync (its next write would otherwise erase the peer's copy) but simply dropped here, because an archive is read-only and never rewritten — and it is not counted as quarantined, since the record was readable and merely has no owner. **Restore needed two explicit wipes that did not previously exist.** `reminderDao.deleteAll()` is called rather than left to the `entries` foreign key's `CASCADE`: the cascade only fires while SQLite's `foreign_keys` pragma is on, and a restore that silently kept every reminder of the replaced dataset keeps firing its alarms. `entryCompletionDao.deleteAll()` has no cascade to fall back on at all — **`EntryCompletion` has an `entryId` index but no foreign key**, so without it a restore leaves every old completion orphaned against entry ids the archive has just reassigned. That one would have stayed invisible: nothing in the app reads completions yet (T9 unbuilt), so it would have surfaced only when a history screen shipped. Widening the two DAO interfaces cost six test-double updates across five files, which is the honest price and still the right trade. 2 new tests (both archive files present in the zip and a tombstoned reminder surviving the round trip still tombstoned; a twice-imported archive not duplicating completions). 478 JVM tests, 0 failures; Android, Windows and `shared` build; `tools/audit.py` clean. **S2 is now complete against both halves of its acceptance test.** | 
| §9.4 unknown-field stripping | A record this build reads *well enough* is republished with the fields it did not understand stripped out | **Fixed — preservation done; detection deliberately separate** | Prior art first, since there was no obvious answer to copy: **protobuf's unknown-field preservation applies only to binary serialisation — unknown fields are lost converting to JSON** — and "plain JSON and JSON Schema have no built-in resolution mechanism; compatibility depends entirely on convention". CouchDB and Automerge sidestep it by storing whole documents rather than typed rows, which is exactly what a Room-backed app cannot do. The lossy typed-row round trip *is* the root cause, so the only question was where the unrecognised bytes live between arriving and leaving: the database (a raw-JSON column on ~14 entities, another schema version), the folder, or nowhere. **The folder won**, and the case it fails to cover is the case where there is nothing to cover — it preserves a field only while the folder still holds the record carrying it, but if the folder no longer holds that record, no peer would have received the field either. It also does not foreclose the database version, which can be layered on later without undoing any of it; the reverse is not true. **The rule is "not declared", not "not present", and that distinction is the fix.** kotlinx.serialization omits a field equal to its default, so a value this build has legitimately *cleared* is absent from the outgoing JSON exactly like a field it never knew about. "Copy any key the folder has that we don't" would restore deleted data — a preservation fix that silently corrupts. Only keys absent from the record's serializer descriptor are carried. Recursive and matched by `uid`, so blocks, properties, views and canvas nodes are all reached (page files are the deepest shape and carry the actual note content); `FormattingSpanSnapshot`, `ViewFilterSnapshot` and `CanvasEdgeSnapshotRecord` have no uid and are left alone rather than matched by index, since a list that gained or reordered an element would graft a field onto the wrong object. **Preservation can never fail a write** — an unreadable, missing or wrongly-encrypted folder file yields nothing and the write proceeds as before, because letting a damaged folder file stop this device publishing is a worse failure than the one being fixed. Cost: the write pass now reads before it writes, already-O(N) in page files becoming O(N) reads too; S6's per-page debounce is the item that addresses that, not this one. 6 tests, including the cleared-value trap and one proving the carry survives *repeated* passes — a fix that only worked on the first pass would decay silently, the same shape of failure as the original bug. 491 JVM tests, 0 failures; Android, Windows and `shared` build; `tools/audit.py` clean. **Detection is deliberately not in this change.** §9.4's record says the defect "failed silently at both ends"; preservation fixes the loss, and reporting that a peer wrote fields this build cannot read is a separate smaller change needing a channel through `SnapshotMergeResult`. Recorded rather than half-done. | 
| B6, B7 | A bound Recurrence cell shows `Period.toString()`'s raw ISO form ("P7D"); a numbered list item's prefix is its position among every block on the page, not its position in the visual list | **Fixed — both done** | **B6**: `Property.kt`'s own `formatPeriodAsInterval` — already fixed for this column's *filter* value (its doc comment names the exact bug: `valueForCell` read "1:WEEK" while the cell itself read something else) — never reached the cell's *display*, since `RecurrenceCell` (`PageDatabaseScreen.kt`) and `RowRecurrenceEditor` (`PageDetailScreen.kt`) both called `Period.toString()` directly. Refactored the shared `count`/`unit` decomposition out into `periodAsCountUnit` and added `formatPeriodAsHumanInterval` ("Every week" / "Every 3 days") alongside it, so the machine and human forms can't drift apart the way the original two independent implementations already had once. Both call sites now use it. **B7**: `BlockPrefix`'s `NUMBERED_LIST_ITEM` branch read `block.order + 1` — order among every block on the page, so a list starting after a heading numbered from wherever `order` happened to land instead of 1. `OutlineBlock` (`BlockOutline.kt`) gains a `listPosition` field, computed once in `outlineOf` by a new `withListPositions` pass: a run of consecutive same-depth `NUMBERED_LIST_ITEM` entries counts from 1, any other block (including a depth change) resets it. `outlineOf`'s own ordering guarantee — siblings under different parents are never adjacent without an intervening block — means a plain "same type, same depth as the previous outline entry" check is sufficient; nothing needed to know about parents directly. Threaded through `BlockRow`/`BlockPrefix` as a plain `Int` alongside the existing `depth` parameter. Verification: Android + desktop compile clean, full JVM suite 469 tests / 0 failures (11 new: 3 for `formatPeriodAsHumanInterval` in the existing `IntervalFormatTest`, 5 for `listPosition` in `BlockOutlineTest` covering a plain run, a list after a heading, a mid-list deletion renumbering what follows, a run broken by an interstitial block, and an independent run inside an expanded toggle), `tools/audit.py` clean. Both were Stage 3 items alongside B3 (2026-09-08, this session), sharing no dependency with it or with each other — picked up as the next small, unblocked items after `P2`/`S4` (below) turned out to be a materially larger piece of work, not a quick follow-on. |
| — | Scoping P2 (`Block.imagePath` rendering) — proceed now, or investigate its actual size first | **Investigated 2026-09-09, deferred** | `build-order.md` lists P2 as depending on B3 and **S4** ("same files" as S2); neither S2 nor S4's real state was on record here. Checked the current tree rather than trusting the plan: **S2 is not done** (`Reminder`/`EntryCompletion` still have no `uid` field, `TendrilDatabase` is still schema version 8 with zero `@AutoMigration`s, no snapshot record/mapper exists for either entity, `PortableArchive`'s file-kind constants don't include them). **S1b is still not done** either, confirming `scope-decisions.md`'s existing precondition entry is still accurate (`fallbackToDestructiveMigration(dropAllTables = true)` still live in `TendrilDatabase.kt`). **S4 itself has nothing built** — no `images/` archive directory, no image file-kind anywhere in `SnapshotSyncOrchestrator`/`SnapshotRecords`/`PortableArchive`, and `Block.imagePath`'s exclusion from the snapshot is explicitly load-bearing, not a stub: `PageSnapshotRecords.kt` and `Block.kt`'s own doc comments call it out, `PagesSyncEngine.kt` actively re-attaches the local path after a merge rebuild, and `PageMergeTest.kt`'s `a local image reference survives a merge that rebuilds its block` test asserts on exactly that boundary ("the merge is the only thing that can carry imagePath across, since the snapshot never holds it"). Implementing S4 for real is medium-to-large, not the `[M]` a one-line build-order row suggests: `PortableArchive.readZipEntries` decodes every zip entry to `String` (`Charsets.UTF_8`), so an image channel needs a second `Map<String, ByteArray>` threaded through `ArchiveContents`, decode, quarantine and the existing 128 MB cap (which an image payload will actually hit, not just guard against); the snapshot folder walker filters strictly to `.endsWith(".json")` and needs a parallel non-JSON listing/merge/purge/lost-marker branch; and the real design cost is that `imagePath` is a device-local absolute path — once it travels at all, the snapshot needs a content-addressed name plus per-device path resolution, which is a different shape from just relaxing the current exclusion, and it inverts the very invariant `PageMergeTest`'s existing test locks in. Desktop has no image handling at all yet either. **Not started** — this is a multi-item, cross-cutting sync-infrastructure change (S2 → S4, plus the design question above), a different order of scope from every other item this session shipped, and left for a deliberate decision rather than picked up under "proceed by dependency." |
| DB8 | `PageDatabaseView.visiblePropertyIds` — the column chooser that does not exist | **Build — done** | `ViewConfigSheet` gains a "Columns" checklist (TABLE/GALLERY only, per the field's own doc comment — BOARD's cards and CALENDAR's date list have no column grid), reading the FULL property list so a hidden property stays reachable to re-show, not just the currently-visible ones. Toggling one off is dropped rather than applied whenever it would empty the set: `visiblePropertyIds = emptyList()` is the field's own established convention for "show everything" (so every view created before this shipped keeps showing every column unchanged), and writing the empty set to mean "hide the last column" would silently flip into its opposite. Display side: `PageDatabaseViewModel` gains a `visibleProperties` `StateFlow` (`combine(properties, selectedView)`, same shape as `boardColumns`/`displayedRows` — derived per-view state belongs on the ViewModel, not computed inline in the Composable, both for consistency and so it's unit-testable without a Compose UI harness), consumed by `TableBody` and `GalleryBody`'s render loops. Schema-management call sites (`PropertyHeaderCell`'s rebind candidates, `AddPropertySheet`, `EnableSyncSheet`) keep reading the unfiltered list — hiding a column changes what's *displayed*, not what a property picker can still reach. A hidden property's own header cell (and therefore its "Edit property type"/"Delete property" menu) is consequently only reachable by first re-showing it from the chooser — an accepted, Notion-matching consequence of hiding a column, not a separate gap. The cross-device sync path (`PagesSyncEngine`'s `visiblePropertyUids` uid-translation) already existed and worked before this PR; what was missing was purely the UI. `tools/audit.py`'s now-stale `PageDatabaseView.visiblePropertyIds` baseline entry removed, re-run clean. Three new `WritePathSyncTest` cases: an untouched view still shows every property, hiding one narrows `visibleProperties` and survives a fresh ViewModel reading the same persisted row, and a hidden column reaches a second device. Closes the last item B3 unblocked; `P2` (image rendering, additionally gated on `S4`) remains the one open item from this batch. |
| P1, P3 | `Block.codeLanguage` (shown and settable nowhere) and `Block.calloutColor` (stored, editable nowhere) | **Build — both done** | Both wired through the same seam: `BlockActionSheet` gains a "Language" row (CODE only — a fixed, tappable preset list of lowercase language names matching the Notion importer's own fence-tag shape, plus "Plain text" for `null`; no free-text entry, since §3.1.1 needs no syntax highlighting for v1 and a short tap list beats typing) and a "Color" row (CALLOUT only — a fixed seven-swatch palette, the same shape as `TagColors`'s small fixed set rather than a full picker). Both dismiss the sheet on pick rather than staying open — `block` is a snapshot captured when the sheet opened (`blockActionSheetFor`), not a live reference, so a selection ring drawn against a stale `block` would never visibly move to what was just picked. Display: a CODE block now shows its language (or "Plain text") as a small label above its content; a CALLOUT block's whole row now always carries a tinted background — the first swatch by default before any pick, matching the icon+color design §3.1.1 already called for — rather than only after a color is explicitly chosen. `PageDetailViewModel` gained `setCodeLanguage`/`setCalloutColor`, both plain `launchAndReindex` mutations, no different in shape from `changeType`/`setToggleExpanded`. `tools/audit.py`'s `FIELD_WRITE_ONLY_BASELINE` entry for `Block.calloutColor` removed and the audit re-run clean, proving the new read is real and not vacuous (`Block.codeLanguage` was never in that baseline — the importer's `ParsedBlock.codeLanguage` field already satisfied the audit's naive `.codeLanguage` regex, a false-negative already on record elsewhere in this file). Two new `WritePathSyncTest` cases cover both pickers reaching a second device. Unblocked by B3 (2026-09-08); `DB8` (column chooser) and `P2` (image rendering, additionally gated on S4) remain open. |
| B3 | Database cell and row-property text fields drop characters when a Room write lands mid-typing | **Fixed** | `build-order.md`'s Stage 3 description matched the code exactly: `UnboundCell`'s plain-text branch (`PageDatabaseScreen.kt`) and `RowUnboundEditor`'s plain-text branch (`PageDetailScreen.kt`) both keyed `remember` on the incoming value itself (`remember(value)` / `remember(storedValue)`), so *any* Room emission for that cell — not just an external one — reset the field's local text, discarding an in-flight keystroke if a write landed between two characters. Fixed with the same `lastWrittenValue`-guarded `LaunchedEffect` pattern the block editor's `lastWrittenContent` already uses (`PageDetailScreen.kt`'s `BlockRow`, shipped before this session): `remember` now keys on the cell/property's stable identity, and an effect only pulls the field's text forward when the incoming value differs from what this field itself last wrote — so the field's own fire-and-forget write echoing back through the Flow is a no-op, but a genuinely external write (sync merge, another device) still lands once typing pauses. **Not independently verified against the stated acceptance test** ("type continuously into a cell for 5s while sync runs; no dropped characters") — this project has no Compose UI test harness (no `androidx.compose.ui.test` / Robolectric dependency anywhere in the build), and adding one is a separate, larger infrastructure decision, not part of an `[S]`-sized bug fix. Verified instead by: the fix is structurally identical to the block editor's already-shipped, already-working guard; full compile (Android + desktop) and the full 456-test JVM suite pass with zero regressions; `tools/audit.py` is clean. Unblocks P1, P3 and DB8, all of which Stage 5 gates on B3; P2 additionally needs S4 (image snapshot/export support), which remains undone and out of this fix's scope. |
| DB1–DB5 | Relation, rollup, formula, summary/explain, computed grouping | **Build** — **DB1 done (`b26c92d`), DB2 done (`5595563`), DB3 core done (`d224e4b`), DB3 wiring done, DB4 done, DB5 done** | Reversal of §5.4's original deferral, already recorded 2026-09-06. Zero schema change: relation edges encode into the existing `PropertyValue.value`, rollups compute on read, the expression lives in `Property.config`. Gated on Milestone 0 reaching **every** device first — see the precondition below, confirmed satisfied 2026-09-08 (both installs verified as descendants of Milestone 0's commit). DB1 and DB2 shipped with zero *sync* code changes: a relation cell and a rollup's own definition are both plain strings in columns the sync layer already carries opaquely, so `PagesSyncEngine` needed nothing for either. **DB2 ships under the type name `COMPUTED`, not `ROLLUP`** — ruled 2026-09-08 (see below), and its config is a structured picker descriptor, not yet the expression text §5.4 describes. **DB3 is sliced in two, the same way S1 split into S1a/S1b**: the core evaluator (lexer, parser, type checker, dependency graph with named-path cycle rejection) shipped first, fully tested with nothing wired to a real database yet. **The wiring PR, ruled 2026-09-08 (see below), gives `COMPUTED` a real `ƒ` formula editor over this database's own plain properties and other formula-authored `COMPUTED` properties — cross-formula references and cycle rejection both work — but not yet a relation or a rollup.** Rollups keep using [`computeRollupValue`][com.tendril.app.ui.pages.PageDatabaseViewModel.computeRollupValue] exactly as DB2 shipped it; a `COMPUTED` property's config is tagged (`"formula:"` prefix vs. DB2's untagged three-field string) so the two authoring paths coexist without either one needing to know the other exists. Relation-aware formulas (`relation()`/aggregate functions inside the expression language, or migrating rollups onto it) are explicitly deferred to a later PR, not started. **DB4 (per-column Summary footer + "Explain this value")**: `TableBody` gets a footer row — `NUMBER` and `COMPUTED` columns show `Σ sum · ⌀ average` over the currently *displayed* (filtered) rows, every other type reads as the acceptance test's "empty" case. Tapping a `COMPUTED` cell opens a read-only sheet naming what it was built from — a formula's direct `prop()` references and their current values, or a rollup's related rows and the value read from each — never an editor, matching `ComputedCell`'s existing "no tap-to-edit" rule. **DB5 (formula-driven view grouping)**: `boardColumns` groups by a formula-authored `COMPUTED` property too, not only `SELECT` — its columns are the distinct non-empty values the formula actually produced across the displayed rows (there is no fixed option list the way `SELECT.config` has one), computed via the same row-local, synchronous `resolvePropertyFormulaValue` the evaluator and "Explain this value" already share, so no suspend call is needed inside the `combine` block. **Rollup-driven grouping is explicitly out of scope, not merely unstarted** — a rollup aggregates across a relation, which needs suspend DAO calls per row, and `boardColumns` recomputes on every displayed-row change; the group-by picker (`ViewConfigSheet`) simply never offers a rollup-authored `COMPUTED` property, the same "not yet, needs its own PR" cut `addFormulaProperty` already made for a formula referencing a relation. A `BoardCard`'s "Move…" reassignment menu is hidden for a `COMPUTED`-grouped board — a derived value has no cell to write a new value into, matching `ComputedCell`'s own no-tap-to-edit rule — closing out the DB1–DB5 chain §5.4 originally deferred, short of relation-aware formulas and rollup/formula unification, both still explicitly deferred per the DB3-wiring ruling above. |
| — | DB3 wiring: unify rollups into the formula language now vs. ship plain-property formulas only and leave rollups on their existing picker | **Plain-property formulas only** | A relation's related rows live on a different database; resolving them needs suspend DAO calls, which `FormulaPropertyResolver` — a synchronous, row-local function by design (see `FormulaEvaluator.kt`'s own note) — cannot make. Making it able to would mean extending `FormulaAst`/`FormulaEvaluator`/`FormulaTypeChecker` to special-case `relation()`+aggregate calls, a materially larger and separate piece of work from wiring the already-shipped, already-tested core evaluator up to a real ƒ editor. Chosen over unifying now for the same reason S1/DB1/DB2/DB3-core were each their own PR: the smallest slice that is real and shippable on its own, with zero risk to DB2's already-working rollups. |
| — | DB2's shape: `PropertyType.ROLLUP` now with a rename later, vs. `PropertyType.COMPUTED` now with structured (non-expression) config | **`COMPUTED` now, structured config** | §5.4 states the final shape as one type with two authoring paths onto one evaluator — pickers that write an expression, revealed and editable as text behind a `ƒ` toggle. Building that literally would require at least a minimal expression parser inside DB2, which is DB3's declared scope. Shipping `ROLLUP` now would mean DB3 renaming the enum member and migrating every stored property's config — exactly the churn §5.4's own reasoning was written to avoid. `COMPUTED`'s meaning is *extended* by DB3, not replaced. |
| — | `INTERVAL` in the general New-property picker | **Stays hidden** | Confirmed as intentional, not a gap. §4's reasoning stands: a general-purpose Interval is a second, uglier way to represent a plain number when `NUMBER` exists. The two `filter { it != PropertyType.INTERVAL }` calls in `PageDatabaseScreen.kt` are the design, and removing them would be a regression. |
| X3 | Wallpaper contrast for widgets | **Build the hinted half only** | `WallpaperManager.getDrawable()` is restricted to the default launcher from Android 13 and is genuinely unbuildable here. `getWallpaperColors(FLAG_SYSTEM)` is API 27+, needs no permission, and returns `HINT_SUPPORTS_DARK_TEXT`. Build that; do not plan the sampled half. |
| X2 | Live embed blocks | **Build as static preview cards** | Compose Desktop has no WebView and `PageDetailScreen` now renders on both platforms, so a live embed cannot work on one of its two targets. A preview card renders identically on both. |
| X4 | Desktop App Lock | **Build as a passphrase gate** | `BiometricPrompt` has no JVM equivalent. Passphrase needs zero new dependencies; the Windows Hello / JNA route would contradict the no-non-AndroidX-dependency posture the desktop spec enforces elsewhere. |
| C4 | AI features using the user's own API key | **Build — the exclusion was miscategorised** | Filed under "needs a server" by an audit sentence that bundled it with real-time collaboration. The user's own key going directly to Anthropic needs no Tendril server and no Tendril account — the same shape as the shipped Google Calendar integration. The plan excluded this in one section while scheduling the identical `SecretStore` path in another. |
| C2 | Template sharing | **Build the substitute, not the gallery** | A *hosted* gallery genuinely needs a server and a publishing identity. Export/import of a single template as a `.tendril` file through the existing `PortableArchive`, plus a bundled starter set, needs neither. |

### Still excluded, unchanged

| # | Item | Why |
|---|---|---|
| C1 | Real-time collaboration | Needs a server, accounts and presence. A different product. |
| C3 | Embedded P2P / IPFS sync | Reverses the founding "Syncthing replicates a folder" decision, needs per-ABI native binaries, and is redundant — the existing design works. |
| C5 | Desktop home-screen widgets | Windows offers no surface a sideloaded JVM app can occupy. A tray popup is new design, not a port. |
| X1 | Character-level text CRDT | Every mature implementation is JS or Rust; a KMP consumer needs native binaries for four Android ABIs plus a Windows JVM under the 16 KB page-alignment requirement, and nothing here ships a `.so`. **This does not block per-block merge — they are separate questions.** |

### Open design question — not cut, not scheduled

**Desktop calendar (was X5 and C6).** Neither proposed answer is accepted. A desktop OAuth
loopback flow works but would hold a persistent refresh token on disk, which is precisely
what the Android design was built never to do; emitting `.ics` into a folder is a
degradation, not a design. Ruled 2026-09-08: **the desktop needs a different structure for
calendar, to be designed separately.** Filed here as an open question so it is not mistaken
for either a cut feature or scheduled work.

---

## Preconditions that outrank the features

**No new `PropertyType` member may ship until every device runs Milestone 0.** Before
Milestone 0, a device receiving an enum value it cannot parse aborted the entire sync
pass — pages, entries, habits, purges — rather than degrading one column. After it, the
column is quarantined and everything else syncs. For DB1 this means Milestone 0 must be
*installed* on both machines, not merely merged.

**Room comes off destructive migration before any schema-changing item.** Split in two,
because only one half can be finished responsibly today.

- **S1a — done 2026-09-08.** `exportSchema = true`, and
  `shared/schemas/com.tendril.app.data.TendrilDatabase/8.json` is committed. Eight versions
  had been compiled and none could be migrated *from*, because Room builds an
  `@AutoMigration` by diffing the previous version's JSON and there was none. Both KSP
  targets write one file, verified byte-identical rather than assumed.
- **S1b — not done.** `fallbackToDestructiveMigration(dropAllTables = true)` is still live.
  Removing it is safe while the version does not move, and its value only arrives at the
  first bump — so it belongs immediately before DB6/DB12/DB13, which change the schema, and
  is not a gate on DB1/DB2/DB3, which do not.

**Open, and I cannot close it.** §9.10's acceptance criterion for S1b asks that the
snapshot-restore fallback be exercised once on a *populated* device against a deliberately
broken migration. §9.10's own 2026-09-06 correction records that the path does not do what
the section describes: Restore merges page-shaped data rather than wiping, and the
"snapshot-folder export" recovery it names does not exist. The two cases are
indistinguishable on a fresh install, because a destructive migration has already emptied
Room by the time Restore runs — so testing it the easy way returns green and proves nothing.
This needs real hardware with real data on it, and until then S1b removes a safety net whose
replacement is unverified.

**A related fragility, found while verifying the above.** `room.schemaLocation` is a KSP
argument, not a declared Gradle output. Deleting a committed schema file does not invalidate
any task, so no ordinary build restores it — it comes back only when KSP itself re-runs. A
schema JSON deleted by accident stays deleted through a green build.

---

## Verified findings — 2026-09-08

Measured, not asserted.

**Four fields are stored, synced across devices, and never displayed.** Every reference in
the repo outside build output:

| Field | Entity | Snapshot | Sync mapper | Importer | Composable |
|---|---|---|---|---|---|
| `Block.calloutColor` | 1 | 1 | 2 | — | **0** |
| `Block.imagePath` | 1 | 2 | 3 | 2 | **0** |
| `Block.codeLanguage` | 1 | 1 | 2 | 2 | **0** |
| `PageDatabaseView.visiblePropertyIds` | 1 | — | 2 | — | **0** |

An imported picture is invisible in the app that stores it and travels faithfully to a
second device that also cannot show it.

**A fifth, found 2026-09-08 by the check written to catch this class.** `Tag.color` is
computed from an eight-colour palette whose own comment records tuning every pair to at
least 12 CIEDE2000 apart under normal vision, deuteranopia and protanopia — and nothing
reads it. `PageDetailViewModel` re-reads a tag from the DAO rather than constructing one,
specifically to preserve the derived colour it then does not use. Careful accessibility
work on a value that renders nowhere.

**`audit.py` was weaker locally than in CI.** Its scan walked `.claude/worktrees/`, a
gitignored full copy of the repository, so every symbol counted twice on a developer
machine and once on the runner. The dead-declaration check can never fire on a doubled
symbol, which is the dangerous direction: a local PASS that CI would not give. Fixed by
excluding `.claude`; the check is now stronger locally than it has ever been, and still
finds nothing.

**The mechanism is a tooling gap, not carelessness.** `tools/audit.py`'s dead-declaration
check anchors its regex at column 0 — its own comment says so. Every entity field, member
function and enum member is indented, so the audit has never checked one. It reports clean
because it structurally cannot look where these live.

**A naive fix would still miss them.** `SnapshotMappers` and `PagesSyncEngine` reference
every one of these fields exactly twice, encoding and decoding. Sync makes dead data look
alive, so a "field with no readers" check has to discount the sync lane specifically.

**A recorded limitation, disclosed rather than found by accident: a DB2 rollup cell can go
stale across databases.** `ComputedCell` recomputes when the row it sits on changes, since
that is what its `LaunchedEffect` key observes. It does not recompute when a *related* row's
target property changes on the database the rollup points at — this ViewModel only observes
its own database's `property_values`, so an edit on the other side produces no signal here at
all. Re-opening the page always shows the current truth; a live cross-database update does
not arrive on its own. Accepted for now because full reactivity would mean watching every
possible target row, which "compute on read" was chosen specifically to avoid — and because
DB3's evaluator will need real dependency-graph invalidation for arbitrary formulas anyway,
which is the more natural place to solve this once, rather than build a rollup-specific
partial answer now.

---

## Provenance

The stage-by-stage build order and per-item size estimates live in `build-order.md`,
imported from the planning artifact that produced them. Its per-item claims are `asserted`
in the sense above unless a row here says otherwise.
